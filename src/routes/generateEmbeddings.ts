// import { getEmbedding } from '../openai';
// import { getPineCone } from '../pinecone';

// const pinecone = await getPineCone();

// pinecone.upsert([{ id: `productId-${newProduct.id}`, values: await getEmbedding(productString) }]);

// const variantEmbedding = await getEmbedding(configuration);
